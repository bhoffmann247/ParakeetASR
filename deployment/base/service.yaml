apiVersion: v1
kind: Service
metadata:
  name: crossbow-parakeet-service
  namespace: superpunch
  labels:
    app: crossbow-parakeet-service
spec:
  selector:
    app: crossbow-parakeet-service
  type: ClusterIP
  ports:
  - port: 80
    name: http
    targetPort: 90
